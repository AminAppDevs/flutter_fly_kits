import 'package:flutter/material.dart';

class BlogTopwriterScreen extends StatelessWidget {
  const BlogTopwriterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
