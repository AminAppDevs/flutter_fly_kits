import 'package:flutter/material.dart';

class BlogProfileScreen extends StatelessWidget {
  const BlogProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
