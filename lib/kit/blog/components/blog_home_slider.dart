import 'package:flutter/material.dart';

class BlogHomeSlider extends StatelessWidget {
  const BlogHomeSlider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
